version: '3.8'

services:
  sousouweb:
    build: .
    container_name: sousouweb
    ports:
      - "3000:80"
    environment:
      - API_DOMAIN=localhost
      - AUTHOR_NAME=Yuccc
      - BLOG_URL=https://your-blog.com
      - BLOG_NAME=我的博客
    restart: unless-stopped
